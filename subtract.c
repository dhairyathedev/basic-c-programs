// Aim: Code for subtraction of three numbers
#include <stdio.h>

int main() {
    int x, y, z, output;
    x = 5;
    y = 4;
    z = 3;
    output = x - y - z;
    printf("The negation of three numbers is %d", output);

    return 0;
}
