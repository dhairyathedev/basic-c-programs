// Aim: Area of Rectangle
#include <stdio.h>

int main() {
    int x, y;
    printf("Enter the length of a rectangle: ");
    scanf("%d", &x);
    
    printf("Enter the breadth of a rectangle: ");
    scanf("%d", &y);
    
    printf("Area of rectangle is %d sq units", x*y);
    return 0;
}
