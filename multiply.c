// Aim: Code for multiplication of three numbers
#include <stdio.h>

int main() {
    int x, y, z, output;
    x = 5;
    y = 4;
    z = 3;
    output = x * y * z;
    printf("The multiplication of three numbers is %d", output);

    return 0;
}
